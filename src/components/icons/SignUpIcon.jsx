import React from "react";
import { FaUserPlus } from "react-icons/fa";

const SignUpIcon = (props) => {
	// Reusable sign-up icon for onboarding actions.
	return <FaUserPlus {...props} />;
};

export default SignUpIcon;
