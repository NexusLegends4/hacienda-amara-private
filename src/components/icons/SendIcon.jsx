import React from "react";
import { BsFillSendFill } from "react-icons/bs";

const SendIcon = (props) => {
	// Reusable send icon for submit buttons.
	return <BsFillSendFill {...props} />;
};

export default SendIcon;
