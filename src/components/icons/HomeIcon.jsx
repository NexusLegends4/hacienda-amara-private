import React from "react";
import { IoHome } from "react-icons/io5";

const HomeIcon = (props) => {
	// Reusable home icon wrapper for navigation links.
	return <IoHome {...props} />;
};

export default HomeIcon;
