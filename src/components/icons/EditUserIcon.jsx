import React from "react";
import { FaUserEdit } from "react-icons/fa";

const EditUserIcon = (props) => {
	// Reusable edit-profile icon for account actions.
	return <FaUserEdit {...props} />;
};

export default EditUserIcon;
