import React from "react";
import { IoMdLogIn } from "react-icons/io";

const LoginIcon = (props) => {
	// Reusable login icon wrapper for buttons and links.
	return <IoMdLogIn {...props} />;
};

export default LoginIcon;
