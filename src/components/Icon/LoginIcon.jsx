import React from "react";
import { CiLogin } from "react-icons/ci";
const LoginIcon = (props) => {
	// Legacy icon wrapper kept for the older import path.
	return <CiLogin {...props} />;
};

export default LoginIcon;
