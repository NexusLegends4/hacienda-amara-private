import React from "react";
import { IoHome } from "react-icons/io5";

const HomeIcon = (props) => {
	// Legacy icon wrapper kept for the older import path.
	return <IoHome {...props} />;
};

export default HomeIcon;
